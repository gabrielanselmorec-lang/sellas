# 13/07/2026 - Fix 10 - Instalacao de dependencias e validacao final

## Escopo

Atender ao pedido de instalar o que fosse necessario para rodar o MVP Sellas/bHave e validar os artefatos principais.

## Alteracoes realizadas

- Instaladas e verificadas as dependencias Python do `requirements.txt`.
- Instalado `PyMuPDF` para renderizacao/QA visual do PDF exportado pelo Microsoft Word.
- Confirmado `lxml` funcional no `.venv` com `python-docx`.
- Instaladas dependencias do frontend React/Vite com `pnpm`.
- Adicionados `@types/react` e `@types/react-dom` para o build TypeScript.
- Configurado `frontend/pnpm-workspace.yaml` para permitir o build nativo do `esbuild`.
- Ajustado `frontend/src/App.tsx` para exportar `App` como default, preservando compatibilidade com importacoes internas.

## Validacoes executadas

- `pytest tests/bhave -q`: 13 testes passaram.
- `tsc --noEmit`: validacao TypeScript concluida sem erros.
- `vite build`: build de producao gerado em `frontend/dist`.
- Renderizacao do PDF exportado pelo Word com PyMuPDF: 30 paginas geradas em `docs/rendered_artigo_bhave_word`.
- Inspecao visual das folhas de contato `contact-1.png`, `contact-2.png` e `contact-3.png`.

## Observacoes

- O `pnpm` precisou de aprovacao explicita do build do `esbuild`.
- Para chamadas diretas de `tsc`/`vite` neste ambiente, foi necessario adicionar temporariamente o Node do runtime ao `PATH` da sessao.
