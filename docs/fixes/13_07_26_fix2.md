# 13_07_26_fix2 - Calibracao explicita e versionamento de modelos

## Alteracao

O treino em `backend/app/ml/training.py` agora:

- usa holdout temporal;
- calibra probabilidades por Platt scaling quando ha amostra de calibracao com duas classes;
- registra metadados de calibracao;
- persiste o modelo selecionado com `joblib`;
- grava metadata JSON e indice em `storage/models`.

Arquivos criados:

- `backend/app/ml/model_store.py`

## Motivo

Gerar probabilidades mais interpretaveis e manter rastreabilidade de versoes de modelo.

## Endpoints impactados

- `POST /api/ml/train`
- `POST /api/ml/predict`
- `GET /api/ml/models`

## Teste

Atualizado `tests/bhave/test_training.py`.
