# 13/07/2026 - Fix 14 - Logica matematica probabilistica bHave

## Escopo

Aplicar a fix descrita em `fix_logica_sellas.md`, atualizando a logica matematica da previsao comportamental baseada em dados da bHave.

## Alteracoes realizadas

- Formalizada a variavel-alvo binaria `Y_t,h`.
- Implementada janela futura configuravel, com padrao `next_session`.
- Adicionado `FeatureConfig` com:
  - `recent_sessions`;
  - `recent_days`;
  - `alpha`;
  - `horizon_sessions`;
  - `prediction_window`.
- Adicionadas features sem vazamento:
  - frequencia historica;
  - frequencia nas ultimas N sessoes;
  - media nas ultimas N sessoes;
  - frequencia nos ultimos N dias;
  - media movel de frequencia;
  - media movel de duracao;
  - media movel de intensidade;
  - risco recente exponencial;
  - tempo desde ultima ocorrencia;
  - sessoes desde ultima ocorrencia;
  - taxas recentes de mesmo ambiente, antecedente e consequencia;
  - tendencia recente.
- Implementado baseline historico explicito.
- Mantidos e ampliados os modelos:
  - baseline historico;
  - regressao logistica;
  - Random Forest;
  - XGBoost opcional quando instalado.
- Adicionado suporte explicito a `CalibratedClassifierCV`.
- Mantido fallback de Platt scaling em holdout quando ha pouca amostra.
- Adicionado modelo experimental conceitual de contagem Poisson.
- Ajustada classificacao de risco:
  - baixo: `p < 0.30`;
  - moderado: `0.30 <= p < 0.70`;
  - alto: `p >= 0.70`.
- Adicionadas metricas:
  - PR-AUC;
  - Brier Score;
  - matriz de confusao;
  - precision, recall, F1, accuracy e ROC-AUC.
- Adicionados endpoints:
  - `POST /api/ml/features`;
  - `GET /api/ml/calibration`;
  - `GET /api/ml/feature-importance`.
- Atualizado o frontend React para aceitar os novos campos de previsao.
- Atualizada a aba comportamental do Streamlit com:
  - probabilidade de ocorrencia na proxima sessao;
  - baseline historico;
  - EMA recente;
  - classificacao de risco clinico;
  - formulas exibidas na tela.
- Criada documentacao em `docs/logica-matematica-previsao-comportamental.md`.

## Testes adicionados/atualizados

- Criacao correta da variavel-alvo futura.
- Frequencia recente configuravel.
- Media movel exponencial.
- Baseline historico.
- Brier Score e PR-AUC no treino.
- Retorno de probabilidade entre 0 e 1.
- Classificacao de risco com limiares estritos.
- Endpoint de features.
- Endpoint de calibracao.
- Endpoint de importancia de features.

## Validacoes executadas

- `py_compile` dos modulos alterados: OK.
- `pytest tests/bhave -q`: 14 testes passaram.
- `tsc --noEmit`: OK.
- `vite build`: OK.
- Chamada real com `TestClient`:
  - sync mock: OK;
  - features: OK;
  - train: OK;
  - predict: OK;
  - calibration: OK;
  - feature-importance: OK.

## Observacoes

- A previsao continua sendo ferramenta de apoio e nao substitui avaliacao clinica.
- Janelas em minutos dependem de timestamps consistentes na origem.
- SHAP, sobrevivencia e Hawkes Process ficam preparados como evolucoes futuras.
